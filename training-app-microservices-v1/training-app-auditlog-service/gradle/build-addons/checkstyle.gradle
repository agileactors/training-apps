apply plugin: "checkstyle"

checkstyle {
    toolVersion = '10.12.0'

    reportsDir = file("$project.buildDir/checkstyleReports")
    configDirectory.set(rootProject.file("rules"))
    maxErrors = 0

}